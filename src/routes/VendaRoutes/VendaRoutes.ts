// import express from "express";
// import VendaController from "../../controllers/VendaController/VendaController";

// const router = express.Router();

// const asyncHandler = (fn: Function) => (req: any, res: any, next: any) =>
//     Promise.resolve(fn(req, res, next)).catch(next);

// router.post("/criar", asyncHandler(VendaController.criar));
// router.get("/buscar", asyncHandler(VendaController.buscar));
// router.put("/atualizar", asyncHandler(VendaController.atualizar));
// router.delete("/deletar", asyncHandler(VendaController.deletar));
// router.post("/baixar", asyncHandler(VendaController.baixar));
// router.post("/extornar", asyncHandler(VendaController.extornar));

// export default router;

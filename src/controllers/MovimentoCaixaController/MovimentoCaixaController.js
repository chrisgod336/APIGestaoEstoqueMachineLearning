"use strict";
// import { Request, Response } from "express";
// import MovimentoCaixa from "../../models/MovimentoCaixa/MovimentoCaixaModel";
// class MovimentoCaixaController {
//   // Buscar movimentos de caixa
//   static async buscar(req: Request, res: Response) {
//     const result: any = await MovimentoCaixa.buscaMovimentoCaixa();
//     return res.status(result.result === "success" ? 200 : 400).json(result);
//   }
// }
// export default MovimentoCaixaController;
